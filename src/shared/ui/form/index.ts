import { FormRoot } from "./form";
import { FormButton } from "./form-button";
import { FormInput } from "./form-input";

export const Form = {
  Root: FormRoot,
  Button: FormButton,
  Input: FormInput,
};
